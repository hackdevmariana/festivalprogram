<header class="text-center bg-light py-4">
    <div class="d-flex align-items-center justify-content-center">
        <img src="{{ asset('storage/' . $logo) }}" alt="{{ $name }}" class="me-3" style="width: 80px; height: 80px;">
        <h1 class="fw-bold m-0">{{ $name }}</h1>
    </div>
    <p class="text-muted mt-2">{{ $slogan }}</p>
</header>

